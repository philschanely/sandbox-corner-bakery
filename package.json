{
  "name": "sandbox-corner-bakery",
  "version": "1.0.0",
  "description": "Sandbox site for demonstrating setup in Github. More coming soon!",
  "main": "src/index.html",
  "scripts": {
    "sync": "browser-sync start -s \"src\" -f \"src/**/*.*\"",
    "start": "npm run sync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Phil Schanely",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.7"
  }
}
